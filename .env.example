# EduNexus — copy to .env.local and adjust as needed

# Application
VITE_APP_NAME=EduNexus

# API (set VITE_USE_MOCK_API=false when backend is ready)
VITE_API_URL=/api/v1
VITE_USE_MOCK_API=true

# Development
VITE_ENABLE_DEVTOOLS=true
